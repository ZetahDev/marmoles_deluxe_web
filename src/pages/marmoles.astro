---
import Layout from "../layouts/Layout.astro";
import ProductCard from "../components/ProductCard.tsx";
import SEOSchemaData from "../components/SEOSchemaData.tsx";
import { listStonesFromS3, type Stone } from "../lib/utils/s3Utils";
import { cloudinaryAssets } from "../lib/images/cloudinaryAssets";
import { buildSeoImageUrl, buildCloudinaryImageSet, CLOUDINARY_PRESETS } from "../lib/images/cloudinary";

const title = "Mármoles Naturales Cali - Mármoles Deluxe | Encimeras y Pisos";
const description =
  "Descubre nuestra exclusiva colección de mármoles naturales para encimeras, pisos y proyectos arquitectónicos. Belleza atemporal y elegancia para cualquier espacio.";

const keywords =
  "mármoles naturales Cali, mármol blanco, mármol negro, encimeras mármol Cali, pisos mármol, mármol cocina, mármol baño, mármol italiano, instalación mármol Valle del Cauca";

// Fetch marble data at build time
const marmoles = await listStonesFromS3("MARMOL/ARTEMARMOL");

// Configurar datos para schema.org
const schemaStones = marmoles.map((stone: Stone) => ({
  name: stone.name,
  image: stone.image,
}));

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const pageUrl = canonicalURL.toString();
const heroImageUrl = cloudinaryAssets.home.projectRoyalWhite;
const heroImage = buildCloudinaryImageSet(heroImageUrl, CLOUDINARY_PRESETS.hero);
const metaImage = buildSeoImageUrl(heroImageUrl);
---

<Layout title={title} description={description}>
  <Fragment slot="head">
    <!-- Preload Hero Image -->
    <link
      rel="preload"
      as="image"
      href={heroImage.src}
      fetchpriority="high"
    />
    <!-- SEO Meta Tags -->
    <meta name="keywords" content={keywords} />
    <link rel="canonical" href={canonicalURL} />
    <meta name="robots" content="index, follow" />
    <meta name="author" content="Mármoles Deluxe" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={metaImage} />
    <meta property="og:locale" content="es_CO" />
    <meta property="og:site_name" content="Mármoles Deluxe" />

    <!-- Twitter Cards -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={metaImage} />

    <!-- Geo Tags -->
    <meta name="geo.region" content="CO-VAC" />
    <meta name="geo.placename" content="Cali" />
    <meta name="geo.position" content="3.4516;-76.5320" />
    <meta name="ICBM" content="3.4516, -76.5320" />
  </Fragment>

  <!-- Agregar datos estructurados schema.org para SEO -->
  <SEOSchemaData
    pageTitle={title}
    pageDescription={description}
    pageUrl={pageUrl}
    pageImage={metaImage}
    category="Mármol"
    stones={schemaStones}
  />

  <main class="pt-20 overflow-x-hidden">
    <!-- Hero Banner con Imagen Real de Mármol -->
    <section class="relative h-80 md:h-[450px] flex items-center justify-center overflow-hidden bg-marmoles-black text-white mb-12">
      <div class="absolute inset-0 z-0">
        <img
          src={heroImage.src}
          srcset={heroImage.srcSet}
          sizes={heroImage.sizes}
          alt="Revestimiento de baño premium con Mármol Calacatta natural en Cali"
          class="w-full h-full object-cover opacity-50"
          width="1200"
          height="450"
          fetchpriority="high"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-marmoles-black via-marmoles-black/40 to-transparent"></div>
      </div>
      <div class="container mx-auto px-4 z-10 text-center">
        <h1 class="text-4xl md:text-6xl font-bold tracking-tight mb-2">
          Nuestros Mármoles
        </h1>
        <p class="text-lg md:text-xl text-gray-200 max-w-2xl mx-auto font-light">
          Belleza atemporal, vetas únicas y elegancia natural para tus espacios en Cali
        </p>
      </div>
    </section>

    <div class="container mx-auto px-4">

      <!-- Features como subtítulo -->
      <div class="max-w-5xl mx-auto mb-12">
        <div
          class="bg-gradient-to-r from-gray-50 to-white p-6 rounded-lg shadow-sm border border-gray-100"
        >
          <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
            <div class="flex items-start text-sm text-gray-700">
              <span class="mr-2 flex-shrink-0">✔</span>
              <span>Elegancia única y atemporal</span>
            </div>
            <div class="flex items-start text-sm text-gray-700">
              <span class="mr-2 flex-shrink-0">✔</span>
              <span>Alta durabilidad y resistencia</span>
            </div>
            <div class="flex items-start text-sm text-gray-700">
              <span class="mr-2 flex-shrink-0">✔</span>
              <span>Belleza natural incomparable</span>
            </div>
            <div class="flex items-start text-sm text-gray-700">
              <span class="mr-2 flex-shrink-0">✔</span>
              <span>Transforma cualquier espacio</span>
            </div>
          </div>
        </div>
      </div>

      <div
        class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 xl:grid-cols-3 gap-8 mb-16"
        id="marmoles-grid"
      >
        {
          marmoles.map((stone: Stone) => (
            <ProductCard
              client:idle
              name={stone.name}
              description="Mármol natural de alta calidad"
              images={[stone.image, stone.design]}
              category="Mármol"
              slug={stone.name
                .toLowerCase()
                .replace(/[^a-z0-9]+/g, "-")
                .replace(/(^-|-$)/g, "")}
            />
          ))
        }
      </div>
    </div>
  </main>
</Layout>

<script>
  import { initModal } from "../scripts/modal";
  initModal();

  // --- Abrir modal automáticamente por parámetro 'material' ---
  if (typeof window !== "undefined") {
    window.addEventListener("DOMContentLoaded", () => {
      const params = new URLSearchParams(window.location.search);
      const material = params.get("material");
      if (material) {
        // Disparar evento para abrir el modal del material
        setTimeout(() => {
          const event = new CustomEvent("modal-state-change", {
            detail: { id: material },
          });
          document.dispatchEvent(event);
        }, 500);
      }
    });
  }
</script>
