---
import Layout from "../layouts/Layout.astro";
import ProductCard from "../components/ProductCard.tsx";
import SEOSchemaData from "../components/SEOSchemaData.tsx";
import { listStonesFromS3, type Stone } from "../lib/utils/s3Utils";

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const pageUrl = canonicalURL.toString();
const metaImage = new URL("/images/contactanos-og.jpg", Astro.site).toString();
const title = "Quartzstone Cali - Mármoles Deluxe | Cuarzo de Ingeniería";
const description =
  "Explora nuestra selección de cuarzo de ingeniería, un material no poroso, resistente a manchas y de bajo mantenimiento. Ideal para cocinas y baños de alta durabilidad.";

const keywords =
  "quartzstone Cali, cuarzo ingeniería, cuarzo cocina, encimeras cuarzo Cali, quartzstone blanco, cuarzo gris, resistente manchas, bajo mantenimiento, instalación cuarzo Valle del Cauca";

// Fetch quartzstone data at build time
const quarstone = await listStonesFromS3("QUARSTONE");

// Configurar datos para schema.org
const schemaStones = quarstone.map((stone: Stone) => ({
  name: stone.name,
  image: stone.image,
}));
---

<Layout title={title} description={description}>
  <Fragment slot="head">
    <!-- SEO Meta Tags -->
    <meta name="keywords" content={keywords} />
    <link rel="canonical" href={canonicalURL} />
    <meta name="robots" content="index, follow" />
    <meta name="author" content="Mármoles Deluxe" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:locale" content="es_CO" />
    <meta property="og:site_name" content="Mármoles Deluxe" />

    <!-- Twitter Cards -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />

    <!-- Geo Tags -->
    <meta name="geo.region" content="CO-VAC" />
    <meta name="geo.placename" content="Cali" />
    <meta name="geo.position" content="3.4516;-76.5320" />
    <meta name="ICBM" content="3.4516, -76.5320" />
  </Fragment>

  <!-- Agregar datos estructurados schema.org para SEO -->
  <SEOSchemaData
    client:load
    pageTitle={title}
    pageDescription={description}
    pageUrl={pageUrl}
    pageImage={metaImage}
    category="Quartzstone"
    stones={schemaStones}
  />

  <main class="py-20 pt-24">
    <div class="container mx-auto px-4">
      <h1 class="text-4xl md:text-5xl font-bold text-center mb-4">
        Quartzstone
      </h1>

      <!-- Features como subtítulo -->
      <div class="max-w-5xl mx-auto mb-12">
        <div
          class="bg-gradient-to-r from-gray-50 to-white p-6 rounded-lg shadow-sm border border-gray-100"
        >
          <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
            <div class="flex items-start text-sm text-gray-700">
              <span class="mr-2 flex-shrink-0">💎</span>
              <span>Resistente a rayones y abrasión</span>
            </div>
            <div class="flex items-start text-sm text-gray-700">
              <span class="mr-2 flex-shrink-0">🛡️</span>
              <span>Superficie no porosa</span>
            </div>
            <div class="flex items-start text-sm text-gray-700">
              <span class="mr-2 flex-shrink-0">🧽</span>
              <span>Fácil mantenimiento y limpieza</span>
            </div>
            <div class="flex items-start text-sm text-gray-700">
              <span class="mr-2 flex-shrink-0">🎨</span>
              <span>Amplia gama de colores y diseños</span>
            </div>
          </div>
        </div>
      </div>

      <div
        class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 xl:grid-cols-3 gap-8 mb-16"
        id="quartzstone-grid"
      >
        {
          quarstone.map((stone: Stone) => (
            <ProductCard
              client:load
              name={stone.name}
              description="Cuarzo de alta calidad y resistencia"
              images={[stone.image, stone.design]}
              category="Quartzstone"
              slug={stone.name
                .toLowerCase()
                .replace(/[^a-z0-9]+/g, "-")
                .replace(/(^-|-$)/g, "")}
            />
          ))
        }
      </div>
    </div>
  </main>
</Layout>

<script>
  import { initModal } from "../scripts/modal";
  initModal();

  // --- Abrir modal automáticamente por parámetro 'material' ---
  if (typeof window !== "undefined") {
    window.addEventListener("DOMContentLoaded", () => {
      const params = new URLSearchParams(window.location.search);
      const material = params.get("material");
      if (material) {
        setTimeout(() => {
          const event = new CustomEvent("modal-state-change", {
            detail: { id: material },
          });
          document.dispatchEvent(event);
        }, 500);
      }
    });
  }
</script>
